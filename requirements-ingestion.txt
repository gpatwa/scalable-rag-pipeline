# Orchestration
ray==2.54.0
langchain-text-splitters  # Used by chunking/splitter.py

# Database & Storage
qdrant-client==1.7.3
neo4j==5.16.0

# Cloud Storage — multi-cloud (AWS + Azure)
boto3==1.34.34                 # AWS S3 access & credential chain
azure-storage-blob>=12.19.0   # Azure Blob Storage support
azure-identity>=1.15.0        # Azure Workload Identity / DefaultAzureCredential
adlfs>=2024.2.0               # Ray Data Azure Blob support (az:// URIs via fsspec)

# PDF Engine
unstructured[pdf]==0.12.0
pdfminer.six==20221105
onnxruntime
pillow
# NOTE: matplotlib removed — never imported in ingestion pipeline.
# NOTE: langchain, langchain-community removed — only langchain-text-splitters is used.

# Core Alignment
pyarrow>=14.0.0
numpy<2.0.0
protobuf<5.0.0
packaging>=24.2
langsmith>=0.1.0
